# TechGarage Frontend v4

React 19 + Vite 8 + Tailwind CSS 4.

```powershell
npm install
npm run dev
```

Rotas principais: `/`, `/produto/:id`, `/carrinho`, `/checkout`, `/perfil`, `/pedidos`, `/pedidos/:id` e `/admin/*`.
